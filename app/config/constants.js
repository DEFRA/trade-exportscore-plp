/**
 * Application constants
 *
 * Central place for small, static values used across the app. Keeping these
 * values here makes tests and configuration selection easier and avoids
 * scattering literal strings around the codebase.
 */
module.exports = {
  environments: {
    development: "development",
    production: "production",
    test: "test",
  },
};
